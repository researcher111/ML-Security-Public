# Lab 20 — microfake (deepfakes from first principles)

Instructor notes + incremental build plan. Student-facing page: `microfake.html`.

## Vision
A "microgpt for deepfakes" (cf. `labs/lab-02/microgpt.html`): build the generative
machinery from first principles, with hand-traceable toy numbers and in-browser
runnable demos. Cover the **full arc** (autoencoder → GAN → diffusion), both a
**2D toy** and a **tiny 8×8 image** runnable demo, and a **runnable toy video**
generator. Match lab-02 in length and detail.

## Pedagogical spine
Three families in invented order, each fixing the previous one's flaw:
1. **Autoencoder** — reconstruct your own input; swap decoders to swap identity (the 2017 face-swap).
2. **GAN** — generator vs discriminator minimax; sharper but unstable / mode collapse.
3. **Diffusion** — add noise, learn to reverse it; stable + high quality; the modern frontier. This is the implemented spine.
Then **§5 video** = add the time axis (temporal consistency, motion, temporal attention) → runnable toy video.
Then **§6 detection** = the defender's turn (frequency artifacts, density gaps, learned detectors, the arms race).

## The 2D "Try it" demo (DONE)
Real DDIM diffusion sampling with an **exact closed-form denoiser**: the data is a
Gaussian mixture on the points of a target shape (smiley / spiral / two-moons), so
`E[x0 | x_t]` = responsibility-weighted mean of the data points. No trained weights.
DDIM update `x ← x0hat + (σ_next/σ)·(x − x0hat)`. Verified: avg dist-to-shape ≈ 0.01
(≤ 3·S0) even at 20 steps. Optional stochastic "churn" toggle (Langevin-ish).
This demo is the bridge: real models must **learn** this denoiser (§4).

## Build status
- [x] Scaffold: head (shared base + KaTeX + Prism), TOC (full arc), hero/lede/cite, glossary infra, footer.
- [x] Try-it: 2D diffusion sampler (`viz.js` initDiffusionDemo). Verified.
- [x] §1 What is a generative model? — full content (p(x), sampling vs density, latent space, 3-family roadmap table, TPS).
- [x] §2 Autoencoder — DONE. Objective (reconstruct own input, no labels) + bottleneck/manifold; live-trained 2-8-1-8-2 AE widget (real SGD, verified loss 0.80->0.0006); shared-encoder/two-decoder face-swap; 4-pixel hand-traceable toy walkthrough (orthogonal expression/identity subspaces -> exact swap, verified 0 error); face-swap widget (real linear algebra, 8x8); blurry-L2 -> motivates GANs; annotated code + exercise.
- [x] §3 GAN — DONE. Minimax + non-saturating loss; GAN architecture diagram (G/D + feedback loop); LIVE 2D GAN widget (MLP+Adam+through-D gradient, 8-Gaussian ring, D decision-surface heatmap, mode-collapse visible) verified in node (8/8 modes); inline training-loop code + downloadable gan.py (pure stdlib, verified runs); one-G-gradient hand walkthrough; mode collapse + instability; exercise.
  - Also: fixed KaTeX math (display math must live in <p class="eq">, never <pre>/<code> — KaTeX ignores those). Added .eq/.eq-note styles.
  - Also: shipped runnable downloads — gan.py + autoencoder.py (pure stdlib), with download rows in §2 and §3 (the "micro version, fully runnable + fully readable" requirement).
- [x] §4 Diffusion — DONE (clarity-focused). The asymmetry (destroy=free, create=hard); forward process x=x0+σε with a forward-noising 8×8 widget; the regression objective ||D(x,σ)-x0||² and WHY it's stable (no adversary, no mode collapse — the "why diffusion won" point); DDIM reverse = the top demo; LEARNED-denoiser widget (train live then generate, EDM preconditioning) verified in node (avg sample dist 0.038); score-function bridge; one-noise-step + one-DDIM-step hand walkthrough; what-scales-it (U-Net, latent diffusion = stacks on §2, text conditioning); downloadable diffusion.py (pure stdlib, verified avg 0.054 @4k iters); TPS (why many steps) + exercise (what the denoiser stores).
  - NOTE: kept §4 to the 2D LEARNED denoiser for clarity + tractability (live-trainable, verified). The tiny 8×8 *generation* model (offline-trained weights + JS sampler, the full microgpt model.json payoff) is still worth adding as a §4 capstone — deferred. Forward-noising is already shown on an 8×8 image.
- [x] §5 Video — DONE (clarity-focused). Video = one sample from p(video), not 24 from p(image); the flicker problem (independence); runnable side-by-side widget (same motion, naive fresh-noise-per-frame vs consistent shared-noise — isolates the single cause/fix); content-vs-motion split (ties to §2); temporal-attention architecture diagram (per-frame spatial stacks + cross-frame dashed attention); what-scales-it (latent video diffusion, space-time patches/Sora, conditioning); TPS (why long video is hard) + exercise; downloadable video.py (pure stdlib, verified: naive flicker 0.099 vs consistent 0.044).
- [x] §6 Detection — DONE (clarity-focused). Generators leave fingerprints; runnable FFT widget (faint upsampling checkerboard invisible in pixels but a spectral spike — node-verified ~130x peak / 7.5x hi-freq ratio); physical/physiological tells; temporal tells (ties to §5 flicker / video.py); likelihood detector via the §4 denoiser; learned detectors + the generalization arms race; honest conclusion (provenance/C2PA/SynthID beats passive detection — defense-in-depth, the course spine); downloadable detect.py (pure stdlib, verified real->real / fake->FAKE); TPS (detector blind spot) + exercise (generator-as-detector is doomed against a strong generator).
- [x] Assignment + autograder — DONE. "Complete the micro-diffusion model": template `microfake.py` with the two conceptual-heart functions stubbed (precond_target = EDM regression target; ddim_step = reverse sampling step); `test_microfake.py` autograder (2 exact-value unit tests + boundary case + lenient end-to-end generation test, ~29s); `solution/microfake.py` reference (passes 4/4). Part 2 reflection ties build → §6 detection/arms race; bonus (new shape / fewer steps / churn). Rubric to 100 + bonus. Verified: solution passes 4/4, stubbed template fails 0/4 cleanly.

## STATUS: all six teaching sections + assignment COMPLETE. 8 verified pure-stdlib .py (autoencoder, gan, diffusion, video, detect, microfake template + solution, test_microfake). 0 WIP markers. All checks green (JS, IDs, anchors, glossary, math-not-in-pre/code, tag balance).
Possible future polish: tiny 8×8 *generation* model w/ shipped weights (the full microgpt model.json payoff — §4 currently does forward-noising on 8×8 + learned generation in 2D); expand FAQ; wire the lab into labs.html / schedule.html index.

## HPC face-swap companion (PyTorch, real faces) — ADDED 2026-07-07
The §2 face-swap realized on real images, for students to run on the UVA HPC cluster.
- `faceswap.py` — shared encoder + two identity decoders (the 2017 deepfake AE), ~180 readable lines. Device-agnostic (CUDA if present, else CPU ~1 min). Args: `--a`/`--b` (subjects 0-39), `--steps` (3000), `--noise` (0.5), `--zdim` (128), `--out`.
- `faces_all.npz` (1.4 MB, shipped) — 40 Olivetti faces, 64×64 uint8, public-domain research set. Chosen over scraping real modern identities (ethics + redistributable + turn-key/offline). Built via `prep_faces.py` (sklearn `fetch_olivetti_faces`). Default swap pair 7 (woman) <-> 21 (man) = maximally distinct.
- `run_faceswap.slurm` — sbatch job, `--account=ds6042 --partition=gpu --gres=gpu:a100:1`. A100 is required: the NGC `pytorch/2.11.0` container ships CUDA kernels for CC >= 7.5 only, so the general pool's V100s (CC 7.0) crash with `cudaErrorNoKernelImageForDevice` (a40/a6000 also work). PyTorch via the pre-built NGC container module (`module load apptainer/1.4.5 pytorch/2.11.0`; run `apptainer exec --nv $CONTAINERDIR/pytorch-2.11.0.sif python ...`). NO pip install.
- `results.png` (shipped example) — 6 rows: A orig / A recon / A→B SWAP || B orig / B recon / B→A SWAP.
- `README-faceswap.md` — turn-key student steps (sbatch → squeue → scp).
- KEY design fix: latent noise during training (`--noise`, the `code()` fn). Without it each decoder memorizes its own identity's exact codes (recon loss → 0) and the cross-identity swap lands out-of-distribution → grainy static. The jitter forces each decoder to handle a neighborhood of codes → clean swaps. Verified on RTX 3090/a6000/rtxpro6000.
- HPC facts (verified 2026-07-07): login default python3 = 3.6.8 (OK — lab is stdlib); modern python via `module load miniforge/26.3.2` = 3.13.13; torch only via the apptainer/pytorch container (import OOMs on the login node — compute node only). Allocation account = `ds6042`. GPU partitions: `gpu` (a6000/v100), `interactive` (rtx, 12h).
- STILL TODO: wire this into microfake.html §2 as a "Going Further — run it on real faces (HPC)" subsection (currently code+data+README only, not surfaced in the HTML lab page).

## diffusion.py runtime fix — 2026-07-07
Added `--iters` CLI (stdlib argparse); default 12000 → 6000. Convergence sweep (local): 3k→avg 0.068, 4k→0.060, 6k→0.050, 12k→0.027. 6k halves runtime (~265s local, ~2× that on the shared login node) at a quality comparable to the originally-verified bar (~0.054 @4k); tunable up for a crisp fit. Motivation: 12k pure-Python (~9 min local, ~15+ min on login node) is bad login-node etiquette + poor student UX. Comment tells students to bump `--iters` on a compute node for sharper samples.

## Whole-lab HPC verification — 2026-07-07
All stdlib deliverables run clean on the cluster on BOTH system python 3.6.8 and miniforge 3.13.13: autograder 4/4 vs solution, 0/4 vs template; autoencoder/video/detect fast; gan ~4 min on login node (borderline — prefer compute node), diffusion now 6k default. Best-case seamlessness: the stdlib lab needs zero `module load` / zero install on the cluster's default python.

## Full real-face arms-race arc + in-page handoffs — ADDED 2026-07-07
Pedagogy: lecture the idea, then students run a real-image PyTorch version on HPC. Each §2-6 has a "Now you run it · (HPC)" handoff (`.callout.hpc-run`, styled in styles.css; lecture beat + copy-paste `sbatch run_hpc.slurm <script>` + "what to look for" + example PNG + downloads). All on the SAME shipped Olivetti faces so the arc closes.
- §2 `faceswap.py` — shared encoder + 2 decoders (done earlier).
- §3 `gan_faces.py` — DCGAN generates faces; saves gan_fakes.npz. Verified: 16 diverse faces, no collapse.
- §4 `diffusion_faces.py` — EDM diffusion generates faces; saves diff_fakes.npz. NEEDED a real residual U-Net (GroupNorm + ResBlocks + 8×8 self-attention) + EMA + zero-mean/unit-var standardization + Heun sampler to stop producing mud; final loss 0.089, samples clearly face-like (v1-v3 with a plain conv stack were muddy — the missing piece was normalization+residual+attention, NOT more steps).
- §5 `video_faces.py` — trains an AE, renders a face-morph two ways: consistent trajectory vs per-frame independent noise. Verified flicker 0.038 vs 0.111 (2.9×). Ships clip_consistent.png / clip_naive.png.
- §6 `detect_faces.py` — CNN real-vs-fake; trains on GAN fakes, tests on unseen diffusion fakes. Verified: 98.8% in-generator, 50.6% (chance) cross-generator — the arms-race generalization gap. Saves spectra.png (real vs fake avg FFT, fake shows hi-freq fingerprint).
- `run_hpc.slurm` — generic GPU runner: `sbatch run_hpc.slurm <script> [args]` -> `apptainer exec --nv $CONTAINERDIR/pytorch-2.11.0.sif python <script> ...`. Account ds6042.
- Example PNGs (results/gan_samples/diff_samples/clip_*/spectra) shipped in the folder and shown in the handoff blocks. HTML cache-bust: styles.css?v=7, viz.js?v=8.

## §4 learned-denoiser WIDGET fix (in-browser) — 2026-07-07
User reported the "train, then generate" widget produced a scattered blob, not the ring. Root cause: converged far too slowly — it lumped 160 samples into ONE Adam step (96k iters = only 600 optimizer updates; avg-dist-to-ring stuck ~0.25-0.31 at realistic interaction time). Fix (viz.js initLearnedDiffusion): (1) 10 small-batch Adam steps of batch 16 per frame instead of one batch-160 step (10× the updates, same compute); (2) EDM log-normal sigma sampling (P_mean −1.2, P_std 1.2) instead of log-uniform; (3) lr 2e-3 → 5e-3. Node-verified convergence: 2s→0.067, 4s→0.050, 6s→0.043 (was 0.25+). Caption "~10–20 seconds" → "a few seconds".

## Readiness polish pass — 2026-07-08
Full-page review before ship. Fixes:
- FAQ Q1 was FALSE post-companions ("not real people") — now discloses the Olivetti faces (40 consenting research volunteers, AT&T 1992–94) and why they're the ethical choice. Added FAQ "Do I need a GPU or the HPC cluster?".
- Moved §3's hpc-run block below the live GAN widget + §3.3 (it referenced the ring widget and mode collapse before the reader met them); moved §4's below §4.4 (it referenced the U-Net before its definition). Handoffs now close their sections.
- HPC blocks: added the missing one-time upload step (§2 shows `scp … :~/lab-20/`; later blocks say "same setup as §2"); replaced `~/…/` placeholder paths with `~/lab-20/`; dropped redundant login-node `module load` (run_hpc.slurm loads it); quoted the `clip_*.png` scp glob (unquoted breaks zsh); §6 now says to run the 3 jobs sequentially (detector reads the generators' fakes) and where accuracies print.
- Clarity: GAN toy gradient now shows the chain rule (`−D′/D = −(1−D)·2`) with an eq-note instead of the cryptic "D′-path"; toy-swap notation `g_B = …` → `g_B(0.8) = …`; §4 asymmetry callout de-duplicated ("trivial … is trivial"); §5.2 "leaks" → "varies"; §2 `--noise 0` claim tempered to match observed output (grainy, not static).

## Interactive-diagram + annotated-code pass — 2026-07-08
Survey found 5 concepts with only prose/equation (no interactive third view); built 4 (skipped §1 sampling-vs-density as lowest-payoff):
- §2.2 `#viz-l2-blur` — drag one prediction against two sharp modes; L2 curve bottoms at the blurry mean (min 0.5, loss 0.09 node-verified). The GAN motivation, made physical.
- §3.1 `#viz-minimax` — two sliders (D-on-real, D-on-fake) → the objective's two log-terms as bars + who-pushes-what; equilibrium button (V = 2·log½ ≈ −1.386 verified). Built because a reader (the instructor!) asked what the symbols meant.
- §4.3 `#viz-ddim-step` — number-line stepper pinned to the callout's numbers; sequence 0.2 → 0.44 → 0.62 → 0.74 → 0.80 node-verified.
- §6.4 `#viz-detect-click` — background-trains a denoiser (IntersectionObserver-triggered, 60k samples ≈ 6s, same K16/lognormal/lr5e-3 recipe as the §4 widget), then click → noise-and-reconstruct verdict. IMPORTANT algorithm note: single correction-vector magnitude FAILS at saddle points (between the moons both pulls cancel → tiny correction → false "real"); multi-sigma mean also fails. What works: Monte-Carlo min over 12 noise-and-denoise trials ("nearest thing the model believes in"), sigma 0.3, verdict bands real<0.12 / fake>0.18 / else borderline. Node-verified: ON points ≤0.092, OFF ≥0.106 (the 0.106 is a point only ~0.26 off-manifold that reads real — kept, it IS the §6 blind-spot lesson and the caption points at it).
- Annotated code (lab-02 hover contract, generalized to multiple blocks via data-panel): §2.2 autoencoder loss+swap (`#ae-annotated`), §3.2 GAN training loop (`#gan-annotated`, the through-D gradient line explained), assignment stubs (`#mf-annotated`, contract not answers). Highlighter + wiring ported into viz.js initAnnotatedCode; CSS into styles.css; docstring lines use class="doc".
- §2 HPC block: full Rivanna connection guide (UVA Anywhere VPN prerequisite, ssh + Duo, login-vs-compute node, `mkdir -p` before scp — the old instruction scp'd into a nonexistent dir), plus interactive-shell alternative (`srun -A ds6042 -p interactive --gres=gpu:a100:1 ... --pty bash`; the allocation flags are battle-verified, the --pty variant is standard but was NOT live-verified — socket was down). NB: a100 pin is required — the pytorch/2.11.0 container has no kernels for the V100s (CC 7.0) in the general gpu pool. README-faceswap.md got the same VPN/mkdir/upload preamble.
- Prose fixes from reader questions: "spending code on identity" → bottleneck-dimension phrasing (2 places; "code" collided with source-code sense); 4-pixel toy now says each entry is one pixel's BRIGHTNESS (scalar, not a coordinate pair).
- Cache-busts: styles.css?v=8, viz.js?v=10. All node-verified; tags balanced.

## Face-swap widget rebuilt as a staged pipeline — 2026-07-08
Instructor feedback: the encoder was an unexplained text arrow, and it was ambiguous whether the sliders move weights or inputs. `#viz-face-swap` is now three labeled panels: (1) INPUT — source canvas + sliders, note says sliders pose the input pixels and never touch the network; (2) SHARED ENCODER — a live mini-net SVG built in JS (squares = pixels, edges = frozen weights drawn once, mouth-pixel edges darker since the expression weights only read those, two red code neurons filling with sign-colors + live values as you slide); (3) TWO DECODERS — both outputs, same z, swap red-outlined. Same math underneath (encode/render untouched); new `updateEnc()` refreshes the code neurons on every input. CSS: `.fs-pipe/.fs-panel/.fs-note/.fs-svg-*` replace `.fs-stage/.fs-arrow`. SVG coords hand-checked for overlaps per repo rule.
Follow-ups (same day): identity dropdown labels were stale ("round"/"square" — both faces have the same square frame; identity is the EYE pixels) → now "A (tall eyes)" / "B (eyebrows)", note ties this to the 4-pixel toy's eyes-vs-mouth split. Added a "meet the identities" reference strip before the widget (`.fs-meet`, canvases fs-idA/fs-idB): both faces at neutral z=0 with a dashed accent box over the eye region (rows 1–3, cols 1–6 — covers A's eyes and B's brows), caption states the frame+mouth are pixel-identical. Cache-busts now styles.css?v=10, viz.js?v=12.
Follow-up (same day): the "meet the identities" strip moved INSIDE the four-pixel toy-walkthrough callout, right after the pinned vectors, and the drawn faces now literally carry those numbers — each eye's two pixels are a_A = [1, 0.6] (A, already true) and a_B = [0.6, 1] (B, was "eyebrows" 0.95/0.35/0.2). Dropdown labels are now "A (eyes [1, 0.6])" / "B (eyes [0.6, 1])"; the dashed eye box tightened to rows 2–3 (no brow row); the walkthrough's closing line points at "the 8×8 faces you just met". Swap math re-verified in node with the new B (orthogonality, exact z recovery, mouth-pixel equality). Cache-busts now styles.css?v=11, viz.js?v=13.
Follow-up (same day, reader feedback ×3): the walkthrough got a four-pixel anatomy figure (inline SVG in the callout, after the "two ingredients" paragraph): 2×2 grid mapping e₁/e₂ (dark identity cells) and m₁ = +z / m₂ = −z (sign-tinted mouth cells) onto the face geometry, caption ties it to the 8×8 dashed-box split. Plus a "why divide by 2" paragraph after the eq-note: m₁ and −m₂ are two readings of the same smile, (m₁−m₂)/2 averages them, and (w·x)/(w·w) in general is the least-squares fit of x ≈ z·w. HTML-only (inline SVG + reused .arch-band/.arch-label/.fs-meet-cap), no cache-bust needed.
Earlier follow-up (same day, reader feedback ×2): (1) The walkthrough's math wall rewritten for readability — one idea per paragraph (face = identity + smile, then orthogonality, then the projection), the encoder's dot-product arithmetic shown in an eq-note instead of left to the reader, and the worked swap now a numbered 3-step hand-trace (build / encode / decode-through-Bob). Same numbers, no content dropped. (2) `#viz-face-swap`'s three panels now fit ONE row: `.fs-panel` flex basis 250→180 (the wrap decision uses hypothetical size, so basis 250 ×3 + arrows overflowed the ~660px viz and dropped panel 3), gap 8→6, panel padding tightened, `.fs-outs` switched to a vertical column so both 120px decoder faces fit a ~200px panel, sliders narrowed to 96px with `.df-field` allowed to wrap. Cache-busts now styles.css?v=12, viz.js?v=13.

## §3 GAN section polish — 2026-07-08
- Minimax equation is now a hover-to-explain widget (`#minimax-eq`, `.eq-hover`/`.eq-part` in styles.css, `initMinimaxEq` in viz.js): five KaTeX pieces (min_G / max_D / real term / + / fake term), hover-focus-tap swaps an explanation into a reused `.code-explain-panel`; injected text is re-rendered with `renderMathInElement` since auto-render only runs at load. Static parts use `$\displaystyle...$` so limits render below the operators.
- New "why the log?" insight callout after the plain-language paragraph: (1) with logs, max_D is exactly BCE / max-likelihood; (2) d(log p)/dp = 1/p gives a confidently-wrong critic an unbounded corrective gradient; (3) at the optimal D the value is 2·JSD − log 4, tying the equilibrium V ≈ −1.39 to the mm-widget's equilibrium button.
- Reader question "does D see the real x?" — yes (first BCE term), and the diagram already drew it, but the `real x` square sat visually INSIDE the Generator band with the dashed G-training-signal curve crowding its label. gan() in viz.js now stops the G band before the x̂/real-x column (both samples sit between the networks), raises the real-x square to clear the dashed curve, and right-aligns its label to the left of the square. Coordinates hand-traced.
- `results.png` rows now labeled in-page: `.row-labeled` wrapper + six `.row-tag` chips pinned at fixed percentages (grid is 6×64px rows + 6px gaps = 414 units, so centers are 7.73/24.64/41.55/58.45/75.36/92.27%); swap rows get accent-red chips; figcaption simplified to point at rows 3 and 6. The PNG itself is unchanged (labels are HTML, so student-generated PNGs still match the docs).
- Cache-busts now styles.css?v=14, viz.js?v=15. node + tag-balance verified.
- The "why the log?" paragraph slimmed on reader feedback (was three numbered arguments with a formula each): now just BCE-familiarity + the 1/p gradient, with the Goodfellow/JSD result compressed to one formula-free parenthetical ("measures a true statistical distance ... provably converges to distribution matching"). The −log 4 ≈ −1.39 equilibrium tie-in lives in the figure caption below it.
- The "why the log?" callout now ends with a static SVG of log p on (0,1] (curve computed offline, coordinates hardcoded): flat at (1, 0), red-dashed plunge to −∞ near p=0, accent dot at (½, −0.69) labeled "the coin-flip critic." Caption states every objective term is ≤ 0, names the 1/p slope asymmetry, and re-derives −log 4 ≈ −1.39. Label positions hand-traced against the curve (the top-right annotation lives ABOVE the axis line; the ½ label sits below-right of its dot — both collided with the curve on first placement). HTML-only, inline SVG.
- Non-saturating-loss callout rewritten on reader feedback ("I don't understand it"): now names p = D(G(z)) once, frames both candidate losses as "how hard to push p up at each p," places G on the PLATEAU of log(1−p) at p≈0 (slope ≈ 1) vs the CLIFF of log p (slope 1/p = 100 at p = 0.01), and ties the result to the −1/(p+1e-8) seed gradient in the G step's code. Reuses the §3.1 log-shape figure ("the log shape you just saw, mirrored").
- Minimax explorer (#viz-minimax) description + caption rewritten on reader feedback ("don't understand the tug-of-war"): description now leads with the point (the whole game moves only two visible numbers; term 1 is D's alone; term 2 contains ONE shared number D pulls down and G pulls up — that shared number is the tug-of-war). Caption is now a play protocol (a D turn, then a G turn restricted to the bottom slider — same slider, opposite directions, "that is the game") and explains WHY equilibrium is ½ (indistinguishable fakes force equal scores) before tying −1.39 to the live GAN's flattening losses ("not a winner — a stalemate").
- New "so what actually trains? · sliders vs. weights" note-callout between the minimax explorer and the non-saturating callout, on reader question "why do we need equilibrium — are we updating the weights?": (1) only weights ever change (the §3.2 loop); the sliders are consequences, "training is that widget playing itself, one gradient step per turn"; (2) equilibrium is the two-player replacement for "loss stopped going down" (no shared minimum exists), and the ½ stalemate is the prize — a coin-flip critic certifies indistinguishable fakes. "Success for G is defined as D's failure."
- Minimax two-slider explorer (#viz-minimax) REMOVED on reader feedback ("not useful") — HTML block, initMinimax IIFE in viz.js, and all .mm-* CSS deleted. Its load-bearing content was absorbed into the (kept, renamed) "so what actually trains? · scores vs. weights" callout: the two-visible-numbers framing, the shared-number tug-of-war, the ½ argument, the −1.39 landmark (now tied to real training logs, since the live GAN widget shows modes-captured + D-surface, not V), and a pointer to the D-surface washing out flat at equilibrium.
- Non-saturating callout shortened to ~13-year-old register on reader feedback (p defined with plain endpoints; whisper-vs-100× push; "the worse G is doing, the louder the coaching"). §3.2 intro paragraph likewise rewritten ("takes turns"; G improves by ASKING the critic — backprop carries "which way should each pixel move to score more real?" through D into G; "D is only consulted, never changed").
- Cache-busts now styles.css?v=15, viz.js?v=18.
- New #viz-nonsat widget after the non-saturating callout (reader request "a visual and an interactive diagram"): both generator loss curves over p = D(G(z)) as an SVG (curves drawn once), one slider + an "early training (p = 0.01)" button; per-curve dots with TANGENT segments whose steepness is the push, readout compares |1/(1−p)| vs |1/p| and their ratio (99× at p=0.01), caption gives the ratio formula (1−p)/p. Geometry node-verified (dots in-plot at extremes; curves cross at p=½ with equal pushes). Deliberately tighter than the removed minimax explorer: one slider, one comparison.
- "Scores vs. weights" callout shortened to ~13-year-old register on reader feedback (halved; "So when is training done?"; −1.39 and the flat-½ pointer moved into a closing parenthetical). viz.js?v=20.
- Non-saturating callout + #viz-nonsat description/caption rewritten once more into a single sustained metaphor (reader: "write as for a 13 year old"): p is the GRADE the critic gives a fake; the loss is a COACH deciding how loudly to yell at each grade; log(1−p) "whispers at the failing student and screams at the star, which is backwards"; the fix's volume is 1/p. Widget readout labels aligned ("at grade p · volume from ..."). viz.js?v=21.
- §4.3 "(this exact update is DDIM)" rephrased on reader feedback (acronym dropped cold): DDIM is now INTRODUCED as a label for the just-stated guess/step/lower/repeat loop, with the Song et al. link and the expansion, and "the name is just a label for this exact update."
- §4.3 sampling-start notation clarified on reader question ("is N(0, σ_max² I) correct?" — yes): inline parenthetical now decodes the square (N takes a variance) and the per-pixel independence.
- score-function gloss expanded on reader question ("where does ∇log p come from?"): names nabla (not delta), and grounds the "three equivalent views" claim with Tweedie's formula — ∇log p(x_σ) = (E[x₀|x_σ] − x_σ)/σ², ε̂ = −σ·∇log p. viz.js?v=22.
- §4.2 loss equation gained two eq-notes on reader request: (1) the ‖·‖² bars = squared vector length (pixel-wise subtract, square, sum) + the E subscript recap; (2) posterior-mean reminder — the loss's optimal answer is E[x₀|x_σ], the likelihood-weighted average of all clean images consistent with the noisy one, tying back to §2's MSE-loves-averages blur and forward to why diffusion steps are small.
- §4.1 forward widget description: "This direction is free — you never need a model to add noise" rephrased on reader feedback → "no training, no network — adding noise is plain arithmetic anyone can do. It's removing noise that will need a learned model (§4.2)."
- §4.1 forward-process equation gained an eq-note on reader question ("is the noise multiplied by e?"): ε is epsilon, not Euler's e — a fresh random noise image, per-pixel N(0,1); σε = "static scaled by the volume knob σ, nothing raised to any power."
- §2.1 four-pixel walkthrough MATH removed on reader feedback: the x = a_id + z·w equation, the 2×2 anatomy SVG, the projection formula + averaging paragraph, and the 3-step hand swap are all gone. The callout (relabeled "toy setup · identity lives in the eyes, expression in the mouth") keeps only the 4-pixel framing, the pinned vectors, and the meet-the-identities strip, closing with a one-line handoff to the widget. Dangling refs fixed: §2.1 shared-encoder paragraph lost its "(In the toy walkthrough … projection w …)" parenthetical; the widget note now says "four-pixel setup above."
- "Toy walkthrough · one G gradient, by hand" callout (σ(2x−1) critic, −1.29 gradient) REMOVED on reader feedback — redundant now that the non-saturating callout + #viz-nonsat widget cover the same gradient story at the right altitude; nothing referenced it.
- TPS Think step clarified again (reader: "there are no numbers in the picture"): now states the number→color mapping explicitly — D's opinion per spot IS a number in [0,1] and the color displays it (warm ≈ 1, cool ≈ 0) — and asks "what number is D forced to output ... and what single color does the whole picture become?" Pair step adds "since ½ is halfway between the warm and cool ends of the scale."
- The §3 "critic's map" TPS Think step now opens with the widget legend (orange dots = real data, dark dots = G's fakes, background = D's opinion) since the TPS asks for predictions before the reader reaches the widget's own description.
- The §3 "critic's map" TPS simplified to the same plain register (D's opinion painted per spot; "what one number should D answer everywhere"; mode-collapse Share step now names the 8 blobs and the "modes captured" readout explicitly). Structure/timing pills unchanged.
- Bug: annotated-code explain panels showed raw `$...$` (screenshot: the −log D(real) annotation) — KaTeX auto-render runs once at load, and initAnnotatedCode injects data-explain HTML afterwards. Fixed by re-rendering the panel's text element on every hover-inject (same guarded renderMathInElement call the eq-hover widget uses). Fixes ALL annotated-code panels page-wide. viz.js?v=19.
- Reader question "what does E_{x~data} mean?" — both expectation hover entries now unpack the notation: E = average over random draws, the subscript names what's random and its distribution (~ reads "distributed as"), and in code each term is a minibatch mean. viz.js?v=17.
- Reader follow-up "does D see ALL the x values, x1 and x2?" — the diagram drew fake x̂ as TWO nodes but real x as ONE square, implying D gets less of the real sample. gan() now draws real x as two data squares (matching the two-component x̂) with a two-line label "real x / (x₁, x₂)"; the Discriminator bullet explains WHY D must see real data (one-class training can't learn a boundary) and that every component feeds its input layer; the diagram caption states real and fake enter D identically. Coordinates hand-traced (squares clear the x̂ circles, the dashed feedback curve, and the edge bundle). viz.js?v=16.

## §4.3 hand-trace: training-data + loss bridge — 2026-07-09
Reader questions: "what is training data" and "how does this relate to the loss." The 1-D walkthrough now pauses right after the forward step: "Pause — you just manufactured one piece of training data" — the training set is a pile of real photos, the noised pixel is a practice question with a free answer key, and the §4.2 loss is the squared error on that question, worked with the callout's own numbers ((0.5 − 0.8)² = 0.09 for a wrong answer of 0.5). "A perfect denoiser" now reads "one that finished all that practice." Also removed the "Before reading the formulas, meet the three characters" lead-in (it sat AFTER the formulas; now opens "In these formulas, x is…").

## §7 TPS integrated into prose — 2026-07-09
On instructor request, the "make the model listen harder" TPS box was dissolved into §7.2's narrative: the dropped-caption fact + the D(c) − D(∅) arrow derivation are now the section's opening two paragraphs ("Look at what that difference is…"), and the Share step's failure-mode prediction became the sentence that launches the widget ("Before you touch the widget, commit to one prediction…"). Note: §7 now has no TPS block (CLAUDE.md's one-per-section default intentionally overridden here — the derivation reads as prose, and the commit-a-prediction beat survives inline).

## §6.4 click-detect widget removed — 2026-07-09
#viz-detect-click (click-to-test denoiser-as-detector, ~120-line initClickDetect IIFE with its background-trained MLP) removed on reader request. The §6.4 prose paragraph stands alone (concept + "run as its own critic"), and the blind-spot argument the widget's caption made lives on in the end-of-§6 exercise ("elegant and doomed"), which references §6.4 the section, not the widget. No CSS was widget-specific (reused df-* classes). viz.js?v=25; node + tag-balance verified.

## §4.3 "two days" callout — 2026-07-09
Reader: "this feels really important but I still don't understand." New insight callout between the DDIM stepper and the learned-denoiser bridge: "training and generating are two different days," with a 5-row scan-back table (what you have / where the noisy x comes from / answer key? / what the network does / what changes) and the punchline "Generation is restoration of a photo that never existed." The bridge paragraph now maps the learned widget's two buttons onto the two days (Train = training day, Generate = generation day). Also §6 opener trimmed on request (dropped "— where this lab pays off for a security course").

## Register sweep, part 2 (viz.js prose) — 2026-07-09
- Glossary entries joined the plain register: latent-space now opens "The model's private shorthand… does its thinking there"; denoiser explains its ideal answer in words (likelihood-weighted average of candidate cleans) before naming the posterior mean. generative-model and score-function left as-is (already plain / intentionally technical per the ∇log p question).
- Post-reorder audit: every "above/below" positional reference in the HTML checked — all correct in the new §4.3 order. viz.js?v=24.

## §4.3 DDIM context + reorder — 2026-07-08
Reader still stalled on the DDIM equation pair ("I need more context of what is happening"). Two fixes: (1) the eq-note grew into two full paragraphs — a "meet the three characters" paragraph BEFORE narrating the formulas (x = the one thing in memory, starts as static; x̂₀ = the guess, hat = estimate; σ→σ_next = rungs of a noise ladder written down in advance) and a narrated turn-of-the-loop with a concrete fraction (0.6 kept = 40% of the gap closed). (2) Section reordered so the numbers arrive immediately: equation → characters → hand-trace callout (0.2 → 0.44) → number-line stepper → THEN the "one missing ingredient" bridge → learned-denoiser widget. Previously the learned widget sat between the equation and the worked numbers.

## Whole-lab plain-register sweep — 2026-07-08
Reader directive: "rewrite all of lab 20 so a 13-year-old could understand," master-teacher voice. §3, §4 (mostly), and §7 were already converted by the day's question-driven edits; this pass swept the rest. Changes are sentence-level only — every widget, equation, TPS, gloss, id, and class untouched:
- Lede: "A deepfake is not magic. It is the output of a generative model — a system that studies thousands of real faces until it can produce brand-new ones that never existed."
- Try-it caption: "One honest disclosure: nothing here was trained…"
- §1: p(y|x) decoded in words; "two superpowers" (Sampling / Scoring); latent space intro ("Nobody models a million pixels directly… does its thinking in code-land"); one-map callout gains the read-aloud ("draw a list of random numbers z, push it through G, out comes an image").
- §2: ‖·‖² decoded at its FIRST appearance (the AE loss); identity-agnostic paragraph rebuilt around "every slot in the code is precious… it says how the face is arranged, never whose face it is"; L2 blur intro names L2 and "the loss function is the culprit."
- §3: "rewarded playing it safe" (echoes §2); mode-collapse exercise answer de-densified.
- §6: Fourier spectrum decoded ("a re-plot of the image that shows which repeating patterns it contains"); manifold reminded at §6.4 first use ("§2's thin surface in pixel space where real data lives"); density → "scoring" to match §1.
- §5, assignment, FAQ audited — already in register, unchanged. HTML-only; tag balance + math-delimiter check pass (the one odd $ is bash's $CONTAINERDIR inside a pre, which KaTeX skips).

## §7 · From text to image — 2026-07-08
- New section between §6 and the Assignment (TOC updated): 7.1 conditioning (caption = one more denoiser input; same §4.2 loss with c; CLIP text encoder + cross-attention, both tied back to lab-02's embeddings/attention), a TPS that has students DERIVE guidance from the dropped-caption trick (D(c) − D(∅) is an arrow — exaggerate it), 7.2 classifier-free guidance with the x̂₀-form equation (ε̂-form noted as equivalent), a "scaling up" note (latent diffusion = §2's autoencoder back for an encore; U-Net + CLIP; s ≈ 7), and a threat-model insight callout (cost curve collapsed to a sentence; prompt filters vs paraphrase = the input-channel arms race).
- New #viz-guided widget: two labeled ring clusters ("cat" left accent-orange, "dog" right green), prompt select + guidance slider (0–6) + Generate; exact GMM posterior-mean denoisers (same math as the top demo), guided x̂₀ = x̂₀(∅) + s·(x̂₀(c) − x̂₀(∅)), animated DDIM. Readout: % landed on prompt + cluster coverage k/14.
- Node-verified with a fixed-seed sim: s=0 → 52% obey (chance); s=1 → 100% obey, full 14/14 coverage, samples ON the ring; s=6 → 100% obey but coverage 7/14 — samples abandon the dog-facing side and pile onto the far edge (meanX −0.49 → −0.74). The first caption claimed high s "crowds the middle" — WRONG for this construction; rewritten to the verified one-sided-caricature behavior, which is also the honest analogue of real CFG's saturation/diversity loss. viz.js?v=23.
- §4.3 DDIM equation pair gained an eq-note on reader feedback (the two formulas' roles weren't narrated): left = guess, right = step-and-lower with ← decoded as "replace x with" and σ_next/σ as the surviving fraction of the gap. The following paragraph now opens with the bridge "That loop needs one ingredient: a denoiser" before the exact-vs-learned contrast.
- §4.3 variance parenthetical rewritten plain ("the square isn't doing anything to the noise; N's second slot reports the spread squared"). §4.2 "three equivalent views (x₀/ε/score)" parenthetical removed on reader request (score gloss keeps another anchor).

## One-zip code download — 2026-07-08
- `lab-20-code.zip` now ships in the lab folder (lab-14 convention): all 16 student-facing files — the six pure-stdlib toys, their five `*_faces.py` HPC companions, `faces_all.npz`, both slurm files, `microfake.py` + `test_microfake.py`, `README-faceswap.md`. Excludes `solution/`, `autograder/`, `prep_faces.py`, and all page assets. **Rebuild after editing any shipped file**: `cd lab-20 && rm -f lab-20-code.zip && zip -X lab-20-code.zip <same 16 files>` — the zip is a snapshot, it does not track the folder.
- New "get the code" callout at the top of the page (before §1): `mkdir/wget/unzip` against `https://researcher111.github.io/ML-Security-Public/labs/lab-20/lab-20-code.zip`. §2's HPC step 1 now wgets the zip on the cluster directly (the scp upload step is gone from the page; README-faceswap.md keeps scp as a parenthetical fallback and gained the same wget block). Zip integrity + contents verified; HTML balance OK. HTML-only, no cache-busts.

## Conventions / reminders
- Shared-base chrome (`.toc`/`.toc-title`/`.toc-foot`, present toggle wired by lab-base.js, glossary pattern) — NOT lab-02's bespoke `.layout`/`.meta`.
- KaTeX `$...$` / `$$...$$` for math (loaded in head).
- Cache-bust `?v=N` on styles.css / viz.js after every edit.
- Per CLAUDE.md jargon rule: the audience knows ML; gloss the generative-specific terms (latent-space, denoiser, score-function, generative-model) — already seeded in `viz.js` GLOSSARY.
- Each shipped widget that does real numerics MUST be node-verified before "done" (the 2D demo was).
- Toy-walkthrough principle: pin weights to small hand-chosen numbers reused across every diagram (cf. microgpt's TOY_W).
- Real trained weights for §4/§5 demos: train offline, export `model.json`, verify the JS sampler reproduces it.
